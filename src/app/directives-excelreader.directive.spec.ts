import { DirectivesExcelreaderDirective } from './directives-excelreader.directive';

describe('DirectivesExcelreaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesExcelreaderDirective();
    expect(directive).toBeTruthy();
  });
});
